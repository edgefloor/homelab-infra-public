name: example/vps-home-ddns
description: Ignore alerts generated by the current home WAN address
whitelist:
  reason: trusted home WAN from Cloudflare DDNS
  expression:
{% for host in crowdsec_trusted_dynamic_hosts %}
    - evt.Overflow.Alert.Source.IP in LookupHost("{{ host }}")
{% endfor %}
