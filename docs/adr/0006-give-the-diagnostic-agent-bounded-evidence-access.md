# Give the diagnostic agent bounded evidence access

Scanners provide normalized findings, then the agent chooses read-only checks
through a short-lived capability bound to one target. The gateway offers typed
operations, redacts secrets, limits resources, and records attributable
observations. Mutation stays on a separate allowlisted command path, which
preserves adaptive investigation without granting the agent a general shell
over the homelab.
